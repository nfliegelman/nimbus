# Nimbus: Future Inclusions Log

**Purpose:** the running list of planned improvements, known weaknesses, and the path to automated trading. For the owner and for any AI assistant working on this project. When an item ships, move it to the HANDOFF.md changelog. When a new idea comes up in a chat, add it here so it survives the session.

**Post-audit priority order (audit batch 11, from the AUDIT_TODO section 22 table):** 1 nowcasting + midday cron: SHIPPED IN SHADOW checkpoint 1 day (2026-07-13, v13); the open step is the 30-event CRPS+RPS promotion gate, 2 NBM promotion decision: DECIDED checkpoint 1 (not adopted, pool wins by 0.311 deg, CI excludes zero), 3 bet-confirmation logging, 4 EWMA + CRPS-sigma experiments: RUN checkpoint 1 (both incumbents kept), 5 Student-t tails at 500 tail buckets (349 as of 2026-07-13, realized 2.0%, healthy), 6 privacy/deploy: DECIDED batch 12 (public through paper, private = live gate 5; Pages-via-Actions deferred again 2026-07-13 to owner-at-computer), 7 always-on runner pre-live. Explicit diminishing-returns list: more ensemble providers, extra surface variables, per-bucket CIs, portfolio optimization.

**Note (2026-07-04):** a full technical audit is in progress; `AUDIT_TODO.md` is the working list. Several items below are expected to be re-prioritized by the audit's value-of-information table (AUDIT_TODO section 22). Ideas surfaced during the audit land here as usual.

Priorities are ordered. Do not skip ahead: several items are gated on settled history that does not exist yet.

---

## 1. Now through ~100 settled bets (nothing to build, just watch)

The calibration engine needs food before anything else matters. During this period the owner's only jobs:

- [ ] Paper trade and let settlements accumulate. Bias corrections activate at 5 settled events per city, learned kernel widths at 8, the pooled width at 15.
- [ ] Watch the **Calibration** table on the Results tab. This is the single most important readout. If the 60-70% row actually hits ~60-70%, the model is honest and profit follows from the cost gate. If it misses badly, nothing else matters until it is fixed.
- [ ] Watch the **Learned corrections** table. Any city with a persistent shift above ~1.5 degrees probably has wrong station coordinates. Fix its lat/lon in `CITIES` to Kalshi's actual settlement station (the CLI station, usually the airport ASOS; NYC is Central Park). The correction handles it either way, but fixing the coordinate is cleaner.
- [ ] Watch the **By edge size** table. If 25%+ edges win less often than 8-15% edges, the plausibility cap is earning its keep and thresholds may need tightening.

## 2. Unit scaling plan (the owner's stated intent, with guardrails)

The owner wants to move toward ~$100 units, possibly betting only the 2-3 highest-probability cards per day. The plan is sound ONLY if units scale with bankroll and the by-win-probability table earns it first. Rules any AI should hold the line on:

- 1u should stay near 1-2% of CURRENT bankroll. $100 units mean a $5,000-$10,000 bankroll, not a $500 one. Raise `BANKROLL` and let `BASE_UNIT_USD` follow; never hardcode a dollar unit that breaks the percentage.
- **Manual-money gate (OPERATIVE; approved by explicit owner sign-off 2026-07-16, message "approve the money gate", replacing the original clause whose "Brier below market" requirement checkpoint 1 proved structurally unpassable):** begin manual money at small fixed stakes only when ALL hold on the audit build: (1) 100+ resolved plays; (2) fees-inclusive ROI positive; (3) average CLV positive with its 90 percent bootstrap CI above zero (the pre-registered "edge shows here first" signal); (4) sd(z) within [0.85, 1.15]; (5) neither kill leg fired; (6) the cheap-entry cell verdict has been read (either direction). Then scale in steps: roughly $500 -> $1,250 -> $2,500 -> $5,000 bankroll, doubling only after 50+ live bets at the current level stay green. The live status of all six conditions renders on the Results page.
- Selective high-confidence mode is approved only when the "By win probability" table (shipped v5) shows the 80%+ row with positive ROI over 30+ plays AND stated-vs-actual within ~5 points. High-probability plays win small and lose big; overconfidence there is invisible in win rate.
- Concentrating in 2-3 bets/day raises daily variance: cap total daily exposure around 4-6u early on. (SHIPPED audit batch 8: DAILY_UNIT_CAP=6.0 per target date plus EVENT_UNIT_CAP=2.0 per ladder, after measuring a 54.5u day and 5-play single-ladder stacks.) Note the model keeps learning from ALL cities regardless of which plays the owner actually bets, so selective betting never starves calibration.
- At $100+ units, liquidity starts to matter: 100-140 contracts can eat through resting depth on thin ladders. Manual mitigation: place limit orders at the board price and accept partial fills rather than market-ordering through the book. `MIN_OI` may need raising (300 -> 1000+) at that size.

**Kill criteria (pre-registered audit batch 8; Brier leg amended 2026-07-16 with explicit owner approval, see HANDOFF Decision Log):** at 150+ resolved plays under the audit build, STOP scaling and return the model to the lab if the bootstrap 90% CI on fees-inclusive ROI sits entirely below -8%, or at 150+ clv-bearing plays the bootstrap 90% CI on average CLV per play sits entirely below zero. Passing is not proof of edge; failing is proof enough to halt.

## 2b. Retune checkpoint (after ~100 settled bets)

- [ ] Revisit `PLAY_NET_EDGE`, `EDGE_2U`, `EDGE_1_5U`, `WINPROB_CAP`, `TIER` colors against the by-edge and by-unit tables. This is a with-AI session: bring the repo files plus a copy of `weather_state.json` so the numbers drive the tune.
- [ ] Decide whether the high-confidence bucket (p_win >= 65%) is the best ROI or just the best win rate. If ROI concentrates in mid-probability plays instead, adjust what gets surfaced first.
- [ ] Consider tightening `MAX_LEAD_DAYS` from 4 to 2-3 if the lead-3+ rows underperform.
- [x] **Spread-skill check READ 2026-07-25 at n=804: RELATIONSHIP IS REAL.** corr(logged member sd, realized absolute error) = +0.250, 90 percent bootstrap CI [+0.186, +0.317], excluding zero. Narrowest quartile of forecasts (mean sd 1.36) misses by 1.52 deg; widest quartile (mean sd 3.87) misses by 2.32 deg. Per the registered clause this justifies surfacing sd more prominently on the boards; SHIPPED 2026-07-25 (HANDOFF v6.17): pick cards carry the spread plus a tight/wide tag at the measured quartile cut points, and the Results page recomputes the correlation and per-band average miss live. Caveat for anyone re-reading the tables: every HIGH is lead 0 and every LOW is lead 1 in this dataset, so the by-lead and by-kind splits are the same split and are not two independent confirmations. Original text: **Spread-skill check** (audit batch 4): corr(logged member sd, |realized error|) overall and per lead, needs n>=100. Early read at n=40 was +0.14, too noisy. A real positive relationship would justify surfacing sd more prominently; a null one simplifies the display.
- [x] **Tail audit READ 2026-07-25 at n=1025: NO ESCALATION.** Realized 1.95 percent against a stated 0.44 percent average, Wilson 95 percent lower bound 1.27 percent, which does NOT exceed 2 percent (audit build alone: realized 1.35 percent, lower bound 0.78 percent). The registered branch is therefore the no-escalate one, and Student-t stays unbuilt. The fallback clause says to CONSIDER relaxing the clamp; deliberately not acted on same-day, because TAIL_FLOOR at 1.5 percent sits just above the audit-era realized 1.35 percent and relaxing it would re-open exactly the fake NO-edge on ultra-thin tails that the clamp was built to stop. Any relaxation needs its own registered experiment. Original text: **Tail audit** (audit batch 4): at 500+ buckets stated under 2%, if the Wilson lower bound of realized frequency still exceeds 2%, escalate from the TAIL_FLOOR clamp to a shape fix (Student-t kernel or sigma inflation beyond |z|>2) and retune TAIL_FLOOR. If realized frequency falls back inside the stated range as learned sigma matures, consider relaxing the clamp instead.
- [x] **EWMA bias experiment** (audit batch 9): compare the decaying-average bias correction (Cui et al. style, weight ~0.05-0.10) against the rolling-30 window on held-out settlement MAE per city/kind; adopt only if it wins at 100+ settlements. Literature supports both; our data cannot yet prefer either. **RUN at checkpoint 1 (2026-07-13, n=259 walk-forward):** rolling-30 won (MAE 1.802 vs EWMA 1.857 at 0.05 and 1.822 at 0.10). Rolling-30 KEPT. A 0.2 decay edged it by 0.009 deg but sits outside the registered range; if desired, register 0.15-0.25 as a checkpoint 2 experiment rather than adopting off a post-hoc scan.

- [ ] **CHECKPOINT 2 DOCKET (pre-registered 2026-07-13, owner session; experiments only, no knob moved today):**
  1. **Cheap-entry economics.** Motivating observation, stated with its era split so nobody later mistakes it for a clean signal: plays entered at 0.20 or below are 4/72 (-$147) on the legacy engine and 0/6 (-$63) on the audit build; every other entry band combined is 74/125 (+$309). The legacy portion is explained by the already-fixed tail overconfidence, so the open question is only whether the audit build still misprices cheap entries. GATE: 40+ audit-era plays with entry <= 0.20 OR p_win <= 0.30. TEST: realized ROI and stated-vs-realized win rate in that cell, Wilson bound vs breakeven at the average entry. **REMEDY PRE-COMMITTED 2026-07-16 (owner session), replacing the pick-one-later clause:** if at the gate the cell's fees-inclusive ROI is negative AND the Wilson 90 percent upper bound on its hit rate sits below breakeven-plus-3-points at the cell's average entry, a MIN_ENTRY floor of 0.20 ships automatically in the next session, with a MODEL_VERSION bump; no fresh judgment call, no alternative remedies, no re-scan. If the condition does not hold, the cell stays open and re-reads at 80. Mechanism note (2026-07-16): the model's tail buckets are CALIBRATED in aggregate (stated 14.4 percent, realized 15.2 percent, n=960 audit-era buckets at 5-25 percent stated), while the tails it PLAYS (where it disagrees most with the market) realized 0 of 9: classic winner's-curse adverse selection at the play filter, not a forecast defect. Scanning other cells to rescue the hypothesis is forbidden. **MEASUREMENT CORRECTION 2026-07-28 (no change to the gate, the test, or the remedy):** the cell's second arm was not being evaluated. `resolve_pending` dropped `p_win` when copying a frozen play into its resolved record, so `_cell` fell back to treating the field as absent and the gate silently read "entry <= 0.20" alone. Retention is fixed and `play_pwin` reconstructs the field from the retained raw `mp` for plays already settled, so the cell now reads as registered on the whole history. Cost of the gap: zero, verified rather than assumed. All 83 audit-era settled plays reclassify identically (every play with `p_win` <= 0.30 also entered at or below 0.20), the cell stays at 14/40, and `compute_report` is byte-identical on the committed state before and after. Recorded here because the gate's own definition drifted, which is exactly the kind of silent narrowing pre-registration exists to prevent.
  2. **READ 2026-07-25 at n=804: NOT ADOPTED, rolling-30 KEPT.** All three registered decays beat the incumbent (0.15: 1.7539, 0.20: 1.7507, 0.25: 1.7531 vs rolling-30 at 1.7643) but the best margin is +0.0136 deg with a paired 90 percent bootstrap CI of [-0.0041, +0.0314], which includes zero. For scale that is 3.9x smaller than the docket 4 advantage that WAS adopted, whose CI excluded zero. The registered clause says adopt if it wins, and it wins; the judgment recorded here is that shipping a knob on a margin indistinguishable from zero would burn a MODEL_VERSION era boundary for nothing and erode the standard that made docket 4 credible. Reopen only with a pre-registered CI requirement. Original item: **EWMA decay 0.15-0.25** vs rolling-30, same walk-forward protocol as checkpoint 1, gate 350+ total settlements. Registered because 0.2 edged rolling-30 by 0.009 deg OUT OF RANGE at checkpoint 1; this makes it an honest in-range test instead of a post-hoc adoption.
  3. **Mid-band calibration read (read-only, no knob attached).** Stated 0.40-0.60 buckets realized 0.38 vs stated 0.46 at n=104 on the audit build; recheck at n>=250 with Wilson bars. If still hot after nowcasting promotes (or fails its gate), it becomes evidence for the Student-t escalation already registered at 500 tail buckets.
  4. **Skill-weighted provider pool (owner-suggested 2026-07-16, protocol fixed BEFORE results were examined).** Candidate: per-kind inverse-MSE weights, w_i proportional to 1/(MSE_i + 0.25), MSE_i over the last 60 prior-DATE settlements of that kind; warmup = member-count weights until every provider has 30 prior settlements of that kind. Registered result on all logged data (strict prior-date walk-forward, n=379): MAE 2.066 vs member-count 2.133, advantage +0.068 deg, 90 percent CI [+0.031, +0.105], and +0.085 on the 300 post-warmup records. Chosen from four PRE-STATED candidates in a single pass: equal-per-model was a wash (+0.003), ECMWF-anchored significantly worse (-0.075), so the method discriminates and no cell-scanning occurred. Per-model raw MAE for the record: ICON 2.11, ECMWF 2.64, GFS 2.81, GEM 3.23. **STATUS: ADOPTED 2026-07-25 (v14-skillpool).** The gate below fired on all three legs (199/150 prospective records, prospective advantage +0.0221 deg, full-sample +0.0534 deg with 90 percent CI [+0.0241, +0.0830]) and the pre-committed action was executed as its own single-knob commit with a MODEL_VERSION bump. See HANDOFF v6.13. **ADOPTION GATE, AMENDED 2026-07-16 BEFORE ANY PROSPECTIVE DATA EXISTED (owner directive: production speed; attribution preserved because knobs still ship one at a time, sequenced by evidence readiness rather than by calendar):** ship as its own single-knob mini-checkpoint, with MODEL_VERSION bump, the moment BOTH hold: 150+ prospective records (target after 2026-07-16) with positive prospective advantage, AND full-sample 90 percent CI excluding zero. At ~40 settlements/day that is readable around Jul 20-21. If the prospective advantage is negative at 150, the candidate is retired. Checkpoint 2 re-reads it only if the gate has not resolved by then. The live challenger tally is displayed on the Results page so the gate fills in public.
  6. **Play-selection replay slate (registered 2026-07-25, BEFORE any replayable data existed).** Motivating finding: the forecast layer is already near-optimal (`backtest_models.py`, n=618: champion 1.799 deg MAE, the best alternative weighting ties at 1.794 with a CI spanning zero, every other candidate worse), so forecast swaps cannot move profitability. The measured losses sit in play selection, which was untestable because `resolve_pending` discarded the order book. `book0` now retains the decision board, and `replay_selection.py` replays selection/sizing/pricing configs against it for true fees-inclusive P&L. Registered slate, fixed in code at registration time: champion; MIN_ENTRY at 0.10 / 0.15 / 0.20 / 0.25; p_win floors 0.30 / 0.40; NO-only; YES-only; PLAY_NET_EDGE 0.06 / 0.08; SUSPECT_EDGE 0.15; flat 1u sizing; MIN_OI 1000; lead <= 1; lead <= 2; MAX_ENTRY 0.85; and MIN_ENTRY 0.20 + NO-only. GATE: 150+ replayable plays under the champion config. TEST: fees-inclusive ROI per config with a 90 percent bootstrap CI, champion as the reference row. ADOPTION RULE, pre-committed: a candidate may be proposed for adoption only if it beats the champion on the FULL sample with its ROI CI excluding the champion's point ROI, AND keeps beating it on targets after its own registration date (`--since`), the same prospective discipline that governs docket item 4. Because the slate is deliberately wide, a single full-sample winner is expected by chance; the prospective leg is what separates a rule from an artifact. Adding a candidate later is allowed but its registration date is the date it was ADDED, and it must be recorded here. Adoption still ships as its own single-knob commit with a MODEL_VERSION bump and a Decision Log row. NOTE: this slate does not supersede docket item 1; if the docket 1 gate (40 audit-era cheap-entry plays) fills first, its pre-committed MIN_ENTRY 0.20 remedy executes on its own terms regardless of what this replay shows. **Candidate addition REGISTERED 2026-07-28 (spread-convergence filter; owner go-ahead quoted: "Go, do everything you need to do"):** motivated jointly by the 2026-07-25 spread-skill read (corr +0.250; the widest sd quartile misses 2.32 deg vs 1.52 for the tightest) and by an external bot's claimed convergence trigger (REDDIT_FINDINGS 2026-07-28 addendum). Two candidates entered the SLATE in `replay_selection.py` the same day, thresholds pinned to the already-measured sd quartiles BEFORE any replay data existed so they cannot be shopped: skip plays when decision-time member sd exceeds 2.80 (the measured p75, SPREAD_WIDE), and the stricter variant skipping above 1.69 (the measured p25, SPREAD_TIGHT). Decision-time sd ships in the same commit: `book0` now freezes the decision board's member sd (record-level `sd` is the LAST refresh's; recording-schema change with a 7b note, no MODEL_VERSION bump, the v6.12 precedent). Snapshots written before 2026-07-28 carry no frozen sd; the replay tool reads the record's final-board sd there as a stated proxy and prints the split on every run so the two eras are never blended silently. The slate's existing gate, test, and adoption rule apply unchanged, with the prospective leg for these two candidates reading `--since 2026-07-28`. **Candidate addition REGISTERED 2026-07-29 (six selection candidates; owner requests quoted: "Can we maybe come up with a few more ideas? And honestly, I want to run all of these tests over time so that we can swap to a new one if it turns out that one won off luck" and, on the first three concepts, "let's go with all your recommendation"):** `NO only + entry >= 0.35` and `NO only + entry >= 0.50` (structural favorite-longshot fades, the section 4 direction guard expressed as slate candidates); `skip p_win 0.80-0.90 band` and `p_win >= 0.90 only` (motivated by the mid-band calibration item, docket 3, AND by the 2026-07-29 inspection of the by-p_win table in audit/JULY_REDDIT_AUDIT_FINDINGS.md); `HIGH markets only` (LOW microstructure differs and LOW Buy NO reads negative in both eras, an inspected cell); `no NO fade of modal bucket` (the v6.6 diagnosis: four mid-price fades of the market's favorite each lost by half a degree). HONESTY CLAUSE FIXED AT REGISTRATION: four of the six were motivated partly by inspected retrospective cells, so their FULL-SAMPLE rows are contaminated by construction and can never justify adoption on their own; for all six the binding evidence is the prospective leg, `--since 2026-07-29`. The slate's gate, test, and adoption rule are otherwise unchanged, and the standing rule that an adopted candidate keeps racing (so a lucky winner that decays shows itself, and swapping again still requires a fresh gate) applies as always. **SECOND-BATCH REGISTRATION AND GOVERNANCE EDIT, 2026-07-29 (owner-approved; the session's enumerated proposal was "(a) register the two new candidates from 11.B, (b) demote the five redundant ones, (c) adopt the sharper promotion standard in 11.E at the next governance edit", and the owner's reply, quoted verbatim: "I trust you to go and execute and merge all of the things you recommend based on the things we've talked through here", the v6.24 answering-an-enumerated-proposal pattern):** (1) MOTIVATING DIAGNOSTIC, recorded so the band candidates are read against the right mechanism: across ALL buckets the model stated at 10-20 percent YES, realized frequency is 15.6 percent (n=1249, stated 15.0: calibrated), but the 51 such buckets the engine FADED at stated p_win 0.80-0.90 realized 45.1 percent YES. The band's failure is winner's-curse conditional selection, not forecast miscalibration; full analysis in `audit/JULY_REDDIT_AUDIT_PART2_REVIEW.md`. (2) TWO CANDIDATES REGISTERED, parameters fixed at registration, full-sample rows contaminated by the motivating diagnostic, promotable only on the prospective leg `--since 2026-07-29`: `selection-shrink 0.25` (p_sel = 0.75 x model + 0.25 x market mid for selection quantities only) and `skip |mp-mid| > 0.25`. (3) FIVE ROWS DEMOTED TO SENSITIVITY STATUS (still computed and printed every run, no longer promotable candidates): `p_win >= 0.30` (empirically co-extensive with MIN_ENTRY 0.20 across the entire history to date), `MIN_ENTRY 0.10`, `MIN_ENTRY 0.15` (sensitivity shadows of the docket 1 remedy), `YES side only` (dominated in all evidence), `sd <= 1.69` (rejects roughly three quarters of trades and cannot reach a readable sample at this cadence). Demotion is reversible only by a fresh registration. (4) ADOPTION RULE STRENGTHENED (a tightening, never a loosening): in addition to the existing full-sample-CI-plus-prospective requirement, a candidate may be proposed for adoption only if ALL of the following hold: 60+ replayed plays in the cell the rule actually changes; full-sample paired uplift >= 2 cents per contract with the date-blocked 90 percent CI excluding the champion; prospective paired uplift >= 1 cent per contract with its date-blocked CI excluding zero; candidate max drawdown no worse than 120 percent of the champion's over the same window; and uplift sign stable across the HIGH/LOW split and across calendar halves. A candidate that fails prospectively is retired, not retuned. All bootstraps block by target date. **THIRD-BATCH REGISTRATION, 2026-07-29 (owner request quoted verbatim: "Yes please and can you also make sure to add something similar into the rain model?", answering the proven-cities proposal):** `proven city/kind only` enters the slate: trade only pairs whose PRIOR replayed settlements show positive model-vs-market Brier skill at 20+ buckets, the same walk-forward flag the sizing gate uses. Registered explicitly as the honest form of "only bet the green cities": the by-city ROI leaderboard at n=6-26 per city is payoff-asymmetry variance (its top row, SATX +31.8u, won 46 percent of its bets), and a retrospective city whitelist is the canonical overfit; this candidate instead requires each pair to EARN inclusion on prior data at every date, so it can never peek. Expected early behavior, stated at registration: it trades nothing until pairs prove out within the replayable window, so its row will lag the slate for days to weeks; that is the design, not a defect. Prospective leg `--since 2026-07-29`. **FOURTH-BATCH REGISTRATION, 2026-07-29 (owner go-ahead: "I am happy to go forward with any of your recommendations", answering the brainstorm's enumerated proposals; the same brainstorm DECLINED, with a walk-forward demonstration recorded in the session, the retrospective positive-P&L city whitelist: on audit-era plays it scores -26.6 percent vs the champion's -7.9, and city rank stability across history halves is Spearman -0.51):** `one play per ladder` (the audit batch 8 finding that within-ladder stacking is the real correlation risk; the 2u event cap still admits two plays per settlement number) and `overround <= 0.19` (ladder vig from book0, sum of asks minus 1, a market-quality score from data already held; threshold pinned OUTCOME-BLIND to the measured p75 over 160 book0 ladders, quartiles 0.07 / 0.12 / 0.19, before any replay result was read). Slate is 31 rows. A revision-stability candidate was considered and NOT registered: the decision board is a market's first board, so no prior-board revision exists at replay decision time and the rule would be untestable in this harness; noted so it is not re-proposed without that problem solved. Prospective leg `--since 2026-07-29`. **FIFTH-BATCH REGISTRATION, 2026-08-03 (owner approval verbatim: "Sure", answering the session's enumerated proposal to register the MIN_ENTRY + band-skip hybrid so its prospective clock starts that day):** `MIN_ENTRY 0.20 + skip 80-90` enters the slate (32 rows), the first deliberate HYBRID row: one rule closing both independently diagnosed selection leaks (the docket 1 cheap-entry adverse selection and the 2026-07-29 winner's-curse band). HONESTY CLAUSE FIXED AT REGISTRATION: both parent candidates' full-sample rows are contaminated by inspected retrospective cells, so the combo's full-sample row is contaminated by construction and can never justify adoption on its own; the binding evidence is the prospective leg, `--since 2026-08-03`. Parameters are the parents', pinned at registration, never tunable on the full history. Interaction with docket 1, stated now so no later session has to guess: if the cheap-entry tripwire fires and ships MIN_ENTRY 0.20 into production, this row stays distinct (it additionally skips the stated p_win 0.80-0.90 band), keeps racing unchanged, and its band component would then be the only live difference from the new champion; the strengthened adoption rule (60+ plays in the changed cell, economic floors, drawdown and stability legs, date-blocked CIs) applies verbatim. Further combos remain registrable but each is its own candidate with its own registration date; combining after peeking at the table stays forbidden.
  5. **Run frequency: considered and DECLINED 2026-07-16.** All four model cycles (00z/06z/12z/18z) are already captured by a run shortly after each completes; between cycles the forecast cannot change, so extra runs would reprice a stale forecast against a fresher market, which is adverse selection by construction. Empirically, in ten days of multi-board operation, zero plays have ever frozen after the first evening board. A new run earns its place only by carrying NEW information; the midday shadow run (station observations) is exactly that audition.
  7. **Bet-timing replay (registered 2026-07-28, BEFORE any replayable board tape existed).** Distinct from item 5: that item asked whether to run MORE often and answered no; this asks whether the board we freeze on is the right one, which was never tested. Motivating position, on the record since audit batch 6 and never measured: "the 21:38 UTC board is the information peak for next-day markets; CLV is now the instrument that TESTS this prior instead of arguing about it." Two things blocked the test until now. (a) Bet time is not a setting: plays freeze at the first PLAYABLE board, so the entry board is emergent, and changing it is a behavior change requiring a MODEL_VERSION bump, not a config tweak. (b) There was no counterfactual price: `book0` stored the first board and `buckets[]` the last, so the boards in between were discarded, and the natural entry-board split is far too lopsided to substitute for a replay (audit-era settled plays: 67 on the 12:17 board, 15 on 21:38, 1 on the 16:10 slot, counted without reading outcomes so this registration is not informed by them). The `tape` field now records every healthy board at ~234 B/board, and `plays_logged_at` survives settlement so the entry board is a logged fact rather than a `first_logged` proxy. REGISTERED SLATE, fixed here before any deciding data exists: champion (freeze at first playable board); freeze-at-board-2; freeze-at-board-3; freeze-only-on-the-21:38-board; freeze-only-on-the-12:17-board; best-price-of-first-2-boards; best-price-of-first-3-boards. GATE: 150+ replayable plays under the champion config with a tape of 2+ boards. TEST: fees-inclusive ROI and average CLV per config, each with a 90 percent bootstrap CI, champion as the reference row; report the count of plays each config would have skipped entirely, since a config that trades less is not thereby better. ADOPTION RULE, pre-committed: a candidate may be proposed only if it beats the champion on the FULL sample with its ROI CI excluding the champion's point ROI, AND keeps beating it on targets after 2026-07-28 (`--since`), the same prospective discipline governing docket items 4 and 6. The slate is deliberately wide, so a full-sample winner is expected by chance; the prospective leg is what separates a rule from an artifact. Adoption ships as its own single-knob commit with a MODEL_VERSION bump and a Decision Log row. Waiting later is NOT free and the test must price it: a later freeze forfeits plays whose edge disappears before that board, and the 02:07 board sits after some markets have thinned, so skipped-play counts and any fill-plausibility loss are part of the verdict, not a footnote. If no candidate clears the gate, the champion stands and this item closes with finality rather than being extended.
  8. **Source-consensus shadow (registered 2026-07-29 WITH the enabling logging, before any source_mp-bearing play existed; owner go-ahead "I am happy to go forward with any of your recommendations").** `book0.source_mp` now freezes each core provider's dressed bucket probabilities under the SHARED city/kind calibration (same corr, same sigma, no cross-provider weighting, no nowcast floor, providers under 5 members omitted, AI providers excluded). SUPPORT DEFINITION, fixed now: provider k supports a play's side iff its own probability minus the executable entry minus (half-spread + fee(mid) + 0.01) clears PLAY_NET_EDGE for that side; support counts run over providers present. ARMS: champion (reference); champion AND support >= 3 of 4; champion AND support = 4 of 4. GATE: 150+ champion plays carrying source_mp. PRIMARY: fees-inclusive ROI of retained plays, 90 percent date-blocked bootstrap, champion as reference; SECONDARY: retained-vs-rejected P&L split, opportunity cost of skipped winners, trade-frequency reduction, average CLV, side and city concentration. Paired by construction. ADOPTION AND REJECTION: the strengthened docket 6 standard applies verbatim (economic floors, prospective leg, drawdown and stability legs); an arm whose frequency reduction exceeds 60 percent without a CI-excluded advantage closes with finality. A record missing any provider vector is excluded from BOTH arms and counted.
- [ ] **Hierarchical pooling revisit** (audit batch 9): at 30+ settlements per city, test partial pooling of bias/sigma across cities against the current shrink-toward-zero; adopt only on held-out improvement.
- [x] **Sigma fitting experiment READ 2026-07-25 at n=804: REJECTED, Wang-Bishop KEPT.** Strict prior-date walk-forward with identical warmup, lookback, clamp and default on both arms; the challenger grid-searched sigma over [0.6, 3.0] in 0.025 steps to minimize CRPS on prior rows. Out-of-sample pooled CRPS: incumbent 1.2932, challenger 1.3027, advantage -0.0095 with 90 percent CI [-0.0137, -0.0054]. The challenger is significantly WORSE, not merely no better: fitting sigma to minimize CRPS on its own window overfits it, while second-moment matching generalizes. The registered rule was adopt only on out-of-sample improvement, so this closes. Original text: **Sigma fitting experiment** (audit batch 4): refit per-city/kind and pooled sigma by minimizing the logged CRPS instead of Wang-Bishop moment matching; adopt only if pooled CRPS improves out of sample.

## 3. Play freezing, then a midday refresh run (SHIPPED, audit batch 1)

- [x] Both halves shipped 2026-07-04 in audit batch 1: plays freeze at their first non-empty log, and a third 14:43 UTC capture cron runs just after Kalshi lists next-day markets at 14:00 UTC. See HANDOFF v5.4 changelog. Residual honest caveat: a play frozen on an earlier board can differ from the board the owner actually taps; the real fix is bet-confirmation logging (item 4 below).

## 4. Quality-of-life

- [ ] **Bet-confirmation logging** (added by audit batch 1). The tracker currently scores the first board that showed each play; the owner may act on a later board. A tiny "I took this" tap per card (persisted via a query param, an issue comment, or a committed file) would make tracked P&L exactly equal taken P&L. This is the clean fix for the frozen-board vs live-board divergence.
- [ ] **Pages deploy via Actions** (added by audit batch 1). Deploy-from-branch was observed serving a board one run stale. Deploying docs/ with actions/deploy-pages inside our own workflow makes deploy failures visible as red runs and also unlocks a private repo later (privacy decision, Batch 12).
- [x] **Phone notification of the day's board.** SHIPPED audit batch 10 (Telegram, secrets-gated, non-fatal). The "what changed since yesterday" line shipped as the new-plays-24h header chip. The owner already runs a Telegram bot via Google Apps Script for work tasks; the same pattern works here. Simplest version: the Action posts a short summary (n plays, top card, link to the page) to a Telegram bot after the morning run. Secrets go in GitHub Actions repo secrets, never in code.
- [x] **Plain-English glossary SHIPPED 2026-07-25 (HANDOFF v6.18).** Collapsed block on the Results tab covering Brier/RPS, CRPS, calibration, sd(z), spread, shift and width, CLV, edge vs net, ROI, units, frozen plays, and gating. Renders on the empty day-one page too, since that reader needs it most.
- [ ] **"What changed since yesterday"** line on the bets page (new plays, dropped plays, corrections that moved).

## 5. Bigger forecasting upgrades (only after the model proves itself on paper)

- [x] **Intraday nowcasting for same-day highs. PROMOTED TO LIVE PRICING 2026-07-25 (v15-nowcast-live).** The re-registered gate fired: 55 binding events against a gate of 25, truncated wins mean CRPS 1.2765 vs 1.5778 and the per-event RPS majority 38 to 4 with 13 ties. Calibration impact was measured before shipping: signed bias improves from -0.324 to -0.087 deg across 200 graded events, so the bias learner sees a better forecast rather than fighting the truncation. See HANDOFF v6.15. THE top value-of-information item (batch 9 literature verdict + community validation in REDDIT_FINDINGS.md Q3). **Checkpoint 1 evidence (2026-07-13) sharpened the case:** the final board's market-implied mean beats the model by 0.43 deg point MAE (1.41 vs 1.84, n=259) even while the model's own calibration is clean (sd(z)=1.00, corrections worth +0.22 deg) and the model beats raw NBM and HRRR. That residual gap IS the intraday-observation advantage this item exists to close; it is the single measured weakness left in the system. Staged spec, prerequisites now shipped:
  - Station map: SHIPPED as `STATION_IDS` in code (batch 9), derived from the batch 1 CLI verification.
  - Mechanics: SHIPPED IN SHADOW at checkpoint 1 (2026-07-13, v13). Between 9 and 14 LST, `shadow_pass` pulls `api.weather.gov/stations/{id}/observations` since 07:00 LST, computes the running observed max, truncates every calibrated member (member := max(member, running_max)), and reprices the ladder from the truncated cloud into a write-once PAIRED snapshot beside the untruncated one. Grading attaches at resolve (rps_u/rps_t, crps_u/crps_t on the resolved record); the Results header shows the running tally.
  - Midday cron: SHIPPED, 16:10 UTC, SHADOW-ONLY (`NIMBUS_SHADOW_RUN=1` keyed off the schedule payload): it freezes no plays and refreshes no boards, so trading behavior and every existing measurement are untouched. Normal runs also collect when a city sits in the window (east snapshots on the morning cron, west at midday).
  - GATE READ 2026-07-16 at n=40 graded events: **FAILED AS REGISTERED, NO PROMOTION.** Overall RPS truncated 0.574 vs untruncated 0.573 (truncated better on only 8 of 40); CRPS 1.362 vs 1.409 (better on 15 of 40, most others tied). Mechanism: truncation BINDS (moves the mean by >0.15 deg) on only 9 of 40 days; the other 31 are ties because the running max sat below the whole member cloud. On the 9 binding days the signal is promising but thin: CRPS 1.388 vs 1.584, point error 1.98 vs 2.20 deg, per-event RPS 6 better / 1 worse / 2 tied. Plays stay on untruncated pricing. Shadow collection continues at zero cost.
  - RE-REGISTERED GATE (2026-07-16, decided before any further data): promotion reconsidered only at **25+ BINDING events** (mean shift >0.15 deg, threshold fixed now), requiring truncated to win mean CRPS AND the per-event RPS majority within the binding subset. At ~2-3 binding events/day fleet-wide this reads in roughly 6-8 weeks. If the binding subset also fails at that n, the feature is removed rather than left running.
- [x] **HRRR + NBM blending decision.** Reference LOGGING shipped in audit batch 2: every record now stores NBM and HRRR CLI-day values (`ref`) so their skill vs the pooled ensemble is measurable from settlements. **DECIDED at checkpoint 1 (2026-07-13): NOT adopted.** On 258 paired settlements the pooled ensemble beat raw NBM by 0.311 deg MAE (95% CI [0.126, 0.495]) and beat raw HRRR by more (2.40 vs 1.84 overall). The audit's adoption gate (advantage CI excluding zero at 150+ pairs) is met in the WRONG direction, so this closes with finality rather than deferring. `ref` logging stays on: it is nearly free and a seasonal regime could reopen the question in winter.
- [ ] **AI ensemble providers, evidence-only first (added 2026-07-28 after the stfarm / Predict & Profit review, REDDIT_FINDINGS addendum).** AIGEFS (NOAA's AI-augmented GFS, 31 members) and ECMWF AIFS-ENS (51 members) are now served by the same keyless Open-Meteo ensemble API the model already uses (ids verified live 2026-07-28: `ncep_aigefs025` and `ecmwf_aifs025`). Community corroboration in REDDIT_FINDINGS section 3 (two traders report using AI ensembles). Honest caveats, recorded so this is not oversold: the post-audit priority list already names "more ensemble providers" as diminishing returns, and `backtest_models.py` (n=618) showed the forecast layer near-optimal with the measured leak in play selection, so expected value here is modest. Path if the owner approves: log both providers into `members_by_model` WITHOUT admitting them to the pricing cloud (the batch 2 NBM/HRRR pattern: evidence first), race them in `backtest_models.py` once 150+ settlements carry them, and admit a provider to pricing only through its own pre-registered gate with a MODEL_VERSION bump; the v14 skill-weighting engine would then weight it by measured MSE like any incumbent. Adopting on the strength of a vendor's marketing claim is the exact tuning-by-anecdote this project forbids. **STATUS: EVIDENCE LOGGING SHIPPED 2026-07-28 (HANDOFF v6.23; owner go-ahead quoted in docket 6).** Each provider fetches in its own call so an AI outage can never touch the pricing fetch; summaries merge into `members_by_model` only, and pricing, the integrity gate, the docket 4 tally, and the backtester's incumbent configs are all unit-tested to ignore them. Run provenance logs via metadata ids `ncep_aigfs025` and `ecmwf_aifs025_ensemble` (the divergent spellings are Open-Meteo's own). **ADOPTION GATE, registered 2026-07-28 BEFORE any AI-bearing settlement existed:** admitting any AI provider to the pricing pool may be proposed only when, at 150+ settlements carrying BOTH providers, the `member-count + AI providers` row in `backtest_models.py` beats the champion on full-sample MAE with a 90 percent bootstrap CI excluding zero AND keeps a positive advantage on targets after 2026-07-28. It would then ship as its own single-knob commit with a MODEL_VERSION bump and a Decision Log row, the docket 4 discipline exactly. If the CI includes zero at the gate, the providers stay evidence-only and the question closes until a season boundary reopens it.
- [ ] **KXRAIN evidence shadow (FUTURE 5b; SHIPPED 2026-07-29, evidence only; owner approval quoted: "Yes let's do it. Let's apply the exact same system wherever it applies to this rain model. I want to have a bunch of different test models running similar to how we do for temperature. Run it baby!!!").** Kalshi's daily measurable-rain binaries settle on the SAME CLI reports and stations as the temperature book, and their 20 city suffixes are exactly the CITIES codes (verified live 2026-07-29; NYC rules cite CLINYC, trace counts as 0 so YES means >= 0.01 inch). Each run logs, write-once per city/date at first sighting, the per-provider ensemble wet fractions (measurable threshold 0.254 mm and any-precip variant, same LST windowing as temperature) beside the market's quote, then grades against Kalshi's settlement. NO rain play is ever generated and no temperature path is touched (isolated fetches, non-fatal wrapper, unit-tested). **PRE-NAMED CANDIDATE SLATE, fixed before any graded data existed (expanded same registration day, 2026-07-29, still before any data):** (1) member-count pooled core wet fraction, the champion-designate; (2-5) each core provider alone (gfs, ecmwf, icon, gem); (6) pooled any-precip (wet0); (7) pooled 1.0 mm threshold (wet1: a grid cell drizzling 0.3 mm is not a station gauge recording 0.01 inch, so the threshold mapping is a real question the record should answer rather than assume); (8-9) AIGEFS alone and AIFS-ENS alone (logged as evidence beside the core, NEVER pooled, mirroring temperature's AI isolation); (10) walk-forward inverse-Brier skill-weighted core pool (the docket 4 idea applied to rain, computable offline from the logged per-provider fractions, engaging only once every core provider has 30 prior graded days); (11) walk-forward beta-calibrated pooled wet fraction (raw ensemble fractions are typically overconfident; calibration fitted only on PRIOR days, never its own); (12, added 2026-07-29 same registration day, owner request "add something similar into the rain model" answering the temperature proven-cities candidate: pooled wet fraction scored on PROVEN CITIES ONLY, cities whose prior graded rain days show pooled Brier beating the market mid at 15+ prior days, the subset recomputed walk-forward at every date so no city's own future grades can select it; needs no new logging since city rides on every record). Candidates 10 through 12 are offline computations over logged fields and need no new logging. (13, added 2026-07-29 same registration day: pooled wet fraction scored on the CONFIDENT SUBSET only, city-days where the pooled fraction is at or below 0.20 or at or above 0.80, the cells any future trading rule would actually live in; offline computation, no new logging.) Adding any further candidate records its addition date here. **GATES, registered before data:** first read at 200 graded city-days (whole slate reported, no cherry-picking). **STATISTICS SPEC, fixed at registration:** primary metric is Brier vs the market-mid Brier; secondary are log loss and the Brier skill score against a walk-forward climatology base rate; reliability deciles carry Wilson bounds; and EVERY confidence interval is a block bootstrap by TARGET DATE, because rain is frontally correlated across cities within a day, so the effective independent sample is closer to the number of weather DAYS than the number of city-days. The 200/400 gate counts are city-days, but no verdict may be read from a city-day-level CI. A TRADING PROPOSAL may be drafted only when, at 400+ graded city-days, the champion-designate (or a slate candidate that beat it at the 200 read AND kept winning prospectively after that read) beats the market mid on Brier with the 90 percent date-blocked CI excluding zero AND its decile calibration sits within Wilson bounds; any actual trading then needs its own spec (cost gate, sizing, exposure caps), explicit owner approval, and a MODEL_VERSION bump. If the CI includes zero at 400, evidence-only continues to a re-read at 800 and the question closes at a season boundary if still null. Records carry whatever providers returned (core pooling requires core presence; prices are never logged without a core forecast); the race filters to full-provider rows per candidate and reports the split. Growth ~0.17 MB/month, noted in HANDOFF 7b.
- [ ] **Lead-aware calibration.** Learn bias and width separately for lead 0-1 vs 2-4 once each city has ~30+ settlements. The plumbing (lead is stored on every resolved record) already exists.
- [ ] **Winter variables revisit** (audit batch 9): snow cover and soil moisture only matter for cold-season extremes; reassess in November alongside the seasonal split below.
- [ ] **Seasonal awareness.** Rolling 30-settlement window already adapts slowly to season; revisit whether an explicit summer/winter split earns anything after a few months of data.
- [ ] **Kalshi CLV analog** (added during audit kickoff, mirrors RidgeSeeker's CLV-first philosophy). Snapshot each play's market price at the last run before settlement and report beat-the-close rate and average price movement on the Results tab. A model that consistently beats the closing price is proving edge even before P&L converges. Depends on play freezing (item 3 above) so the entry price is stable; design lands in AUDIT_TODO section 8.

## 6. The path to automated trading (spelled out, in order)

Do not start this until the Results tab shows model Brier below market Brier AND green P&L over 100+ resolved bets. Automation multiplies whatever the model is, including its mistakes.

1. **Read-write API access.** Kalshi issues API keys (RSA key pair) for order placement. Store the private key in GitHub Actions secrets or the runner's environment. Never in the repo, never in `weather_state.json`.
2. **Shadow mode first.** The bot COMPUTES the exact orders it would place (ticker, side, price, count) and logs them without sending. Run this for 2+ weeks and compare shadow orders to what the owner would have tapped. This catches sizing bugs with zero dollars at risk.
3. **Maker orders, not market orders.** (Reinforced by community consensus in REDDIT_FINDINGS.md Q5: maker-only, minimum price ~$0.10, skip books with >10% spread; use the websocket order-book feed, not REST polling; apply for Kalshi's Advanced API tier if hitting 429s.) At 4-14 cent edges, crossing the spread eats 1-3 cents per trade. Post resting limit orders inside the spread and accept partial fills. This alone can be the difference between profitable and not at this scale.
4. **Hard risk rails, in code, before the first live order:**
   - max exposure per market, per city, per day (e.g. 1 play per city/kind/day, max 6u/day total)
   - a kill switch: one env var or repo file that halts all order placement
   - idempotency: every intended order gets a client ID derived from (date, ticker, side) so a rerun can never double-place
   - sanity bounds: refuse any order whose price moved more than X cents from scoring time
5. **Platform move.** GitHub Actions is the right platform for the current paper phase (free, firewall-proof, commit-back persistence) but the WRONG platform for live orders: scheduled runs can be delayed by many minutes and are paused after 60 days of repo inactivity. When real orders flow, move execution to a small always-on runner (a $5/month VPS, Fly.io/Railway free tier, or a Raspberry Pi at home) and keep GitHub Pages purely for dashboards.
6. **Reconciliation.** Every run pulls actual fills from the Kalshi portfolio API and diffs them against intended orders. Any mismatch halts trading and flags the dashboard.
7. **Scale rule.** Live sizing starts at half of paper sizing for the first month. Bankroll percentage sizing only after fees-inclusive ROI is positive live.

## 7. Known weaknesses (honest list, keep it current)

- **Fees vs edge at small size.** Kalshi's fee is largest near 50 cents. At a sub-$500 bankroll, fees plus spread eat a large share of a 4-8 cent edge. The cost gate accounts for this, but it means thin months even when the model is right. This is skill-building scale, not income scale.
- **Competition.** Confirmed and sharpened by REDDIT_FINDINGS.md: wethr.net is the community's standard analytics hub (paid tier sees data 3 minutes faster), and traders report 1-minute-data flash moves near settlement (do not leave resting orders then, live era). Nimbus's edge window is mornings and mid-probability buckets; assume near-settlement prices are efficient.
- **Forecast staleness at run time.** A 7am board is stale by noon. Mitigated by the midday refresh (item 3) and eventually nowcasting (item 5).
- **Station mismatch.** RESOLVED for now: audit batch 1 verified all 20 cities against Kalshi's own rules text (CLI products) on 2026-07-04 and fixed the one mismatch (Houston settles at Hobby, config said IAH). Residual risk is Kalshi changing a settlement station; re-verify if rules text changes.
- **Schedule reliability.** Measured, not just theoretical: in July 2026 the 12:00 UTC cron fired at 13:26 and the 02:00 UTC cron at 05:51 (+1.4h and +3.8h). Crons now sit off the hour and the pages self-flag past 16h, but GitHub remains best-effort: acceptable for paper, not for live orders. Schedules also pause after 60 days without repo activity.
- **Frozen board vs live board.** Plays freeze at their first non-empty log; the owner may bet a later, slightly different board. Conservative direction (the tracker scores the model's earliest call), but real. Fix: bet-confirmation logging (section 4).
- **State file scaling. RESOLVED 2026-07-28 (HANDOFF v6.24): policy amended and implemented.** The ratified batch 3 numbers (3 MB trigger, 120-day window) were never mutually satisfiable, so the policy would have fired and moved nothing; caught with the file at 2.37 MB and roughly five days from tripping, its whole history 27 days old. Now a 6 MB trigger and a 45-day window, both derived from the live-only pricing lookback rather than chosen independently, with `archive_pass` / `load_archive` / `reporting_view` shipped and the split proven lossless and report-identical on the live 963-record state. Still no database. Residual watch item, stated so it is not a surprise later: the archive file grows without bound by design, because it IS the track record. If the repository rather than the working file ever becomes the constraint, the next step is compaction of archived records, never deletion.
- **Rain shadow stale-pending rows (added 2026-07-29, final once-over).** A pending rain record whose market Kalshi never settles (delisting, rules change) would linger in `rain.pending` indefinitely. Harmless bytes at this scale and deliberately NOT auto-pruned: dropping unsettled evidence needs its own registered policy if it ever actually occurs. Watch item only.
- **DST transition days.** Two days a year the LST settlement window behaves oddly. Lowest priority; consider simply not betting those two days.
- **Rule risk.** Kalshi can change fees, bucket structure, or settlement sources. The parser reads strike fields rather than titles, which helps (a community post-mortem lost money misreading B-tickers as thresholds), but any structural change needs a code touch. Reassuring datapoint: the Reddit pass found no voided weather markets or post-hoc settlement amendments as of mid-2026.

---

*Maintenance rule: any AI making changes should read this file along with HANDOFF.md, and move shipped items into the HANDOFF changelog.*
